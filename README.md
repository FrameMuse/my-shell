# Shell

This is a custom shell, based on ubuntu-server and QT that provides desktop, panels, UI components, and a window manager.

## Development

- use `./build.sh` to build the QT app
- use `./shell.sh` to run the app, it sets everything automatically